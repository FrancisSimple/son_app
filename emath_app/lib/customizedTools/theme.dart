import 'package:flutter/material.dart';

class ThemMode extends StatefulWidget {
  final name;
  const ThemMode({super.key, this.name});

  @override
  State<ThemMode> createState() => _ThemModeState();
}

class _ThemModeState extends State<ThemMode> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}